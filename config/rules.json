{
  "_comment": "Block/allow URL rules. Most-specific pattern wins; on a tie, Allow wins. Wildcard syntax: plain domain (+subdomains), *.domain.com, domain.*, or *wildcard*.",
  "rules": [
    { "type": "allow", "pattern": "google.com" },
    { "type": "block", "pattern": "chromewebstore.google.com" },
    { "type": "block", "pattern": "*games*" },
    { "type": "allow", "pattern": "*math-games*" }
  ]
}
